import express from "express";
import cors from 'cors';

const app = express();
app.use(cors());
app.use(express.json());

app.post();

app.get();

app.ṕost();

app.get();

app.ṕost();

app.listen(4000);